import { describe, expect, it } from "vitest";

//this is a test for the main file, it checks if the main file is working correctly
describe("main", () => {
    it("should be true", () => {
        expect(true).toBe(true);
    });
});